# The station runs as radio, not root. Its home (settings, caches) is on the
# writable data partition; audio for the MiniAmp, input for the volume knob.
radio -1 radio -1 * /data/radio /bin/sh audio,input SleepRadioPi station
